import React from 'react';

const NavigationView = (props) => (
  <nav className="navbar navbar-light bg-faded">
    <h1 className="navbar-brand mb-0">Todo App</h1>
  </nav>
);

export default NavigationView;